overwrite: true
schema: "http://localhost:4000"
documents: null
generates:
  src/shared/index.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    hooks:
      afterOnFileWrite:
        - 'prettier --write'
